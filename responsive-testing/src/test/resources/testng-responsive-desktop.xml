<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">
<suite name="Responsive Test Suite — Desktop" parallel="false" verbose="1">

    <listeners>
        <listener class-name="listeners.AnnotationTransformer"/>
        <listener class-name="listeners.TestListenerTestNG"/>
        <listener class-name="listeners.ExtentTestNGListener"/>
        <listener class-name="io.qameta.allure.testng.AllureTestNg"/>
    </listeners>

    <!-- ========== DESKTOP (default browser size, no device emulation) ========== -->
    <test name="Desktop">
        <parameter name="browser" value="chrome"/>
        <classes>
            <class name="TestNG.tests.ResponsiveTest"/>
        </classes>
    </test>

</suite>
