<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">
<suite name="Responsive Test Suite" parallel="tests" thread-count="3" verbose="1">

    <listeners>
        <listener class-name="listeners.AnnotationTransformer"/>
        <listener class-name="listeners.TestListenerTestNG"/>
        <listener class-name="listeners.ExtentTestNGListener"/>
        <listener class-name="io.qameta.allure.testng.AllureTestNg"/>
    </listeners>

    <!-- ========== DESKTOP (default browser size, no device emulation) ========== -->
    <test name="Desktop">
        <parameter name="browser" value="chrome"/>
        <classes>
            <class name="TestNG.tests.ResponsiveTest"/>
        </classes>
    </test>

    <!-- ========== MOBILE (iPhone 12 emulation) ========== -->
    <test name="Mobile (iPhone 12)">
        <parameter name="browser" value="chrome"/>
        <parameter name="device" value="IPHONE_12"/>
        <classes>
            <class name="TestNG.tests.ResponsiveTest"/>
        </classes>
    </test>

    <!-- ========== TABLET (iPad Pro 11 emulation) ========== -->
    <test name="Tablet (iPad Pro 11)">
        <parameter name="browser" value="chrome"/>
        <parameter name="device" value="IPAD_PRO_11"/>
        <classes>
            <class name="TestNG.tests.ResponsiveTest"/>
        </classes>
    </test>

</suite>
